import { UrlHostDirective } from './url-host.directive';

describe('UrlHostDirective', () => {
  it('should create an instance', () => {
    const directive = new UrlHostDirective();
    expect(directive).toBeTruthy();
  });
});
